# AGENTS.md —— 本项目工作规范（给 AI 助手与协作者）

> 本文件是本仓库的**协作契约**：定义文档导航、开发流程、Commit 规范与 README 维护规则。
> 任何在本仓库工作的助手（Pi 等）请先阅读本文件再动手。

## 1. 项目是什么

课程实验项目：用 C 语言实现一个**支持多级目录结构的命令行简单文件系统**
（目录树 + 双哈希表 + KMP 模糊查找 + 多模式遍历 + 递归删除 + 文件读写状态机）。
分为两个实现目录：

- `human_version/` —— 学生自己实现的**简化版**（先跑通，可用 strstr 替代 KMP）
- `AI_version/` —— **升级版**（human 完成后，与用户一起讨论完善点再动手）

## 2. 文档导航（动手前先读）

| 文档 | 角色 | 说明 |
|---|---|---|
| `SPEC.md` | ★ 唯一规格依据 | 精简自老师 docx，含结构图/命令表/细节标注/未定义行为 |
| `DESIGN.md` | 接口约定 | 模块划分、函数签名、关键算法、human/AI 分工建议 |
| `README.md` | 项目主页 + Commit 记录 | **每次 commit 后必须同步更新** |
| `AGENTS.md` | 本文件 | 工作规范 |
| `tests/` | 样例 | `sample_input.txt` 回放、`sample_transcript.txt` 对照 |
| `简单的文件系统-*.docx` | 老师原始文档 | 只读参考，不要修改 |

**冲突裁决**：老师 docx 与 SPEC.md 冲突时，以 SPEC.md 为准（它是实现依据）；发现 SPEC 与 docx 不一致，先与用户讨论再改。

## 3. 核心规则：每次 commit 必须做两件事

> 缺一不可，这是本项目的硬性约定。

### 3.1 commit message 必须详细

格式模板（正文各节可选但鼓励标注，重要变更必须写全）：

```
<type>: <一句话主题>（做了什么）

背景
----
为什么做这件事 / 前置情况。

做了什么
--------
按条目列出具体变更（新增了哪个文件、改了哪个行为、为什么这样设计）。

如何验证
--------
怎么测试/回放（如：回放 tests/sample_input.txt 对照 sample_transcript.txt）。

待确认 / 下一步
---------------
遗留问题、下一阶段计划、未定义行为的决策记录。
```

- type 惯例：`docs:`（文档）/ `feat:`（新功能）/ `fix:`（修 bug）/ `refactor:`（重构）/ `test:`（测试）/ `chore:`（杂项）。
- 一个 commit 只做一件事（一个主题），不要混入无关改动。

### 3.2 每次 commit 必须同步更新 README 的「Commit 记录」

在 `README.md` 的 `## Commit 记录` 一节**头部**追加一条新条目，格式：

```
### YYYY-MM-DD — <与 commit 主题一致的标题>

**做了什么：** （要点化，与 commit message 对应但不重复全文）
**待确认 / 下一步：** （若有）
```

> 规则：**先写 README 更新，再 git commit**，保证「代码变更 + 记录变更」落在同一次提交里。
> Commit 记录按时间倒序（新的在最上面）。

## 4. 开发流程约定

1. **讨论先行**：涉及接口变更、未定义行为决策、SPEC 修改时，先与用户讨论再实现。
2. human_version 阶段：优先保证**跑通官方样例**；可暂时简化（strstr、不查越界、不清理全局内存），简化点必须在 commit 里说明。
3. AI_version 阶段：KMP / 内存安全 / 边界用例 / 自动化测试 / 代码规范，具体清单见 `DESIGN.md §7`，动手前再与用户确认。
4. 每完成一个阶段提交一次，**每次提交都要满足 §3 的两条规则**。

## 5. AI_version 开发模式（当前所处阶段）

> 用户约定（转写）：
> “AI 版本按**新建 Issue 的方式**完成功能改进：① KMP 替换 strstr ② 内存安全（不越界）
> ③ 输入安全 ④ 补齐未定义行为 ⑤ 健壮性增强。实现写在 **AI_version/ 文件夹**，human_version 不动，
> 两版差异即体现 AI 版能力。用户全程不写代码、只负责 review；由我（Pi）写实现并跑通测试；
> 有不清楚的细节必须与用户仔细讨论。”

执行规则：

1. **Issue 驱动**：每项改进先建 GitHub Issue（含背景 / 目标 / 验收标准），实现完成后 close。
2. **代码只落在 AI_version/**：human_version 冻结不再改动；两个版本的差异 = AI 版增强能力的体现。
3. **分工**：Pi 写实现 + 本地与 CI 跑通测试；用户全程只 review，不写代码。
4. **细节讨论**：实现中的设计决策（接口、边界行为、取舍策略）必须先与用户讨论确认，不可擅自决定。
5. **验收闭环**：每个 Issue 完成 = 测试全绿 + 详细 commit（满足 §3）+ push + close Issue。
6. 测试共享：`tests/run_tests.py` 支持指定版本目录（默认 human_version），CI 对两个版本都跑，
   保证 AI_version 每步改进可用同一套 9+ 用例回归。

## 6. 实现时务必遵守的规格细节（速查）

完整列表见 `SPEC.md §5`，以下是最容易翻车的：

- `Dir  xxx` 输出是**两个空格**、`File xxx` 是一个空格。
- `SEARCH RESULTS:` 独立成行，后不跟空格。
- `cd` 成功**无输出**；根目录下 `cd ..` 静默忽略。
- 同名**文件和目录可共存**（查重只查同类哈希表）。
- 重命名**不改变**链表创建顺序位置。
- open 后除 `close_file` / `write_file` 外，一切命令 → `ERROR: invalid operation`；未打开时 `write_file` → 同样报错。
- 未定义行为（见 SPEC §6）的最终决策要**记录在 commit / README 里**，不要默默改。